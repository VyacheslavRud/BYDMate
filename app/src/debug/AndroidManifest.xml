<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Dev is deliberately manual-start only when installed beside stable in a car. -->
    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove" />

    <application>
        <receiver
            android:name="com.bydmate.app.service.BootReceiver"
            tools:node="remove" />
    </application>
</manifest>
