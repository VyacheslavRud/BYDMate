# Handoff — 2026-05-28 (Phase 2 complete, awaiting in-vehicle smoke)

## TL;DR

- **Branch:** `feature/native-data-stack`, HEAD `22ea2df`
- **Version:** `v2.9.0` (versionCode 304)
- **Status:** Phase 2 (native write channel) **code-complete**, signed APK на DiLink (`/sdcard/Download/BYDMate-v2.9.0.apk`), ждёт **in-vehicle smoke** (`docs/native-data-stack/smoke-phase-2-2026-05-28.md`)
- **Релиз НЕ выпущен.** `gh release create` запускать только после smoke PASS + явной команды Andy.

## What got done this session (Phase 2)

Native write channel migration: BYDMate теперь self-sufficient, после успешного smoke юзер сможет uninstall'нуть `com.van.diplus`.

### Architecture

```
VehicleApi.dispatch(command)
   ↓ CommandTranslator (37 Chinese → actionName+value)
   ↓ WriteAllowlist (110 entries: 23 LIVE + 87 crowd-validation, BANNED_DEVS filtered)
   ↓ VehicleWriteError fail-soft (6 states)
   ↓ HelperClient (TCP 127.0.0.1:8765, JSON-line)
   ↓ HelperDaemon (app_process под shell uid, dex spawned via AdbOnDeviceClient)
   ↓ ServiceManager autoservice + Binder transact 6 (setInt)
```

`HelperBootstrap.ensureRunning()` стартует daemon из `TrackingService.onCreate` fire-and-forget — read path не блокируется.

### Files touched (Phase 2 Groups C+D+E)

| Layer | Files |
|---|---|
| Vehicle API | `VehicleApi.kt`, `VehicleApiImpl.kt`, `WriteAllowlist.kt`, `CommandTranslator.kt`, `VehicleWriteError.kt`, `SunroofMode.kt`, `HelperClient.kt`, `HelperBootstrap.kt`, `HelperDaemon.kt` (in `:helper-dex` module) |
| Consumers | `ActionDispatcher.kt`, `AlicePollingManager.kt`, `TrackingService.kt`, `BatteryStateRepository.kt` |
| Persistence | `VehicleWriteLogEntity.kt`, `VehicleWriteLogDao.kt`, `AppDatabase.kt` (v15), `AppModule.kt` (MIGRATION_14_15) |
| Assets | `app/src/main/assets/competitor-actions.json`, `app/src/main/assets/helper.dex.sha256` |
| Tests | `VehicleApiWriteTest.kt`, `VehicleApiFailSoftTest.kt`, `CommandTranslatorTest.kt`, `WriteAllowlistTest.kt`, `Migration14To15Test.kt` |
| Docs | `docs/native-data-stack/plan-phase-2-2026-05-28.md`, `docs/native-data-stack/smoke-phase-2-2026-05-28.md`, this handoff |
| Deleted | `DiParsControlClient.kt` |

### Grep gate

`grep -rn "DiParsControlClient\|sendCmd\|8988\|com\.van\.diplus" app/src/` → **0 hits** в main и test.

### Codex audits

- C.1 → APPROVED W/ FOLLOWUPS → fixes `134803e`
- C.6 → APPROVED W/ FOLLOWUPS → fixes `7c67618`
- D.1+D.2 → APPROVED W/ FOLLOWUPS → docs `33f63f9`
- **Phase 2 final → BLOCKED → fixes `ce67ed0`**:
  1. **BLOCKER:** `HelperBootstrap.ensureRunning()` нигде не вызывался → теперь fires из `TrackingService.onCreate`
  2. `logWrite()` обёрнут в `runCatching` (fail-soft durability)
  3. `ECO模式` явно в DROPPED list (drive mode → BANNED dev 1004)
  4. `validated` колонка теперь `@ColumnInfo(defaultValue="0")` — schema 15.json совпадает с MIGRATION DDL
  5. Smoke checklist table names: `trip_entity` → `trips`, `charging_entity` → `charges`; добавлен HelperBootstrap log check в #0

## Build & deploy state

- `assembleRelease` ✓ → `app/build/outputs/apk/release/BYDMate-v2.9.0.apk`
- Signed via `~/.android/keystores/bydmate-release.jks` (alias `bydmate`)
- v3 signature verified ✓
- Signed APK на Mac: `/Users/mac_andy/Projects/byd/BYDMate-v2.9.0.apk` (50.8 MB)
- Pushed на DiLink: `adb push` → `/sdcard/Download/BYDMate-v2.9.0.apk`
- Andy ставит вручную через файловый менеджер. **НИКОГДА `adb install`.**

## Next actions (in order)

### 1. Smoke (operator step — needs car + Andy)

Чеклист: `docs/native-data-stack/smoke-phase-2-2026-05-28.md`, 9 пунктов (#0–#8).

Критический путь:
1. Установить v2.9.0
2. `adb shell pm uninstall com.van.diplus`
3. Перезапустить TrackingService (или машину)
4. `logcat -d | grep HelperBootstrap` → `ensureRunning → true`
5. Пройти #1–#8: app launch / dashboard / trip / charging / automation rule / Alice / ABRP / diagnostics

**PASS:** только если #0/#1/#2/#3/#4/#7/#8 — ✓. Пункты #5/#6 (конкретные автоматизации/Alice) могут FAIL по crowd-validation gap — это не блокер.

**FAIL критическое (#1–#4/#7/#8):** откатить uninstall (`adb install com.van.diplus.apk` есть в `/Users/mac_andy/Projects/bydmate-alice/`), debug, фикс.

### 2. Release (after smoke PASS + Andy "выпускай")

- Release notes: plain text (UpdateDialog не рендерит md), русский, без упоминания Алисы (скрытая фича). Образец формата: v2.5.12 (см. `feedback_release_notes_format.md`).
- Тема: "Native write-канал, BYDMate работает без D+" или похожее.
- Body: Новое / Исправления / Установка.
- `gh release create v2.9.0 BYDMate-v2.9.0.apk --title "..." --notes "..."`
- НЕ запускать без Andy.

### 3. Merge to main

После релиза — `feature/native-data-stack` сквошится в main одним коммитом (как `feature/autoservice-readonly` ранее). Ветка остаётся на remote как архив.

## Open items NOT addressed

- **#125** (Phase 2 E.4) in-vehicle smoke — operator step
- **#128** Phase 2 C.7 fid-range secondary filter в WriteAllowlist — research, не блокер
- **#67** Этап C "Native write channel" — этой задачей закрывается, можно после smoke перевести в completed
- **#44** Seat heat 3rd-party path — отложено per memory `project_seat_heat_third_party_path.md`
- **#30** S6 sleep probe — deferred (Phase 1 research)

## Key user constraints to preserve

- "Релиз без меня не делать. цель это приложение вот на этом компьютере которую я смогу скопировать уже на машину" — APK на Mac ✓, копия на DiLink ✓, релиз НЕ запускать
- "оставляем влидациюна пользователей пишем все из конкурентов" — crowd validation strategy ✓ (87 не-валидированных entries в WriteAllowlist)
- "Так, не забывай кодекс на аудит на важные части запускать" — codex запускался C.1, C.6, D.1+D.2, Phase 2 final ✓
- subagent code model = sonnet (per `feedback_subagent_code_model.md`) ✓
- APK deploy = adb push в `/sdcard/Download`, никогда adb install ✓
- Release notes: plain text, русский, без Алисы

## Reference files for next session

- Plan: `docs/native-data-stack/plan-phase-2-2026-05-28.md`
- Smoke: `docs/native-data-stack/smoke-phase-2-2026-05-28.md`
- This handoff: `docs/native-data-stack/handoff-2026-05-28.md`
- Memory: `project_bydmate.md`, `project_d_removal_roadmap.md`, `feedback_*.md`
- Reference DBs (testing sync logic): см. `CLAUDE.md` "Reference Databases" section
