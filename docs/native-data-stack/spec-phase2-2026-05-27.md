# Phase 2 — Native write channel for comfort controls

**Status:** draft, awaiting brainstorming + approval
**Author:** generated 2026-05-27 after Phase 1a S9/S10/S5 validation
**Branch:** `feature/native-data-stack`
**Replaces:** existing D+ `sendCmd` path for vehicle comfort actions

---

## 1. Goal

Add a one-way **write channel** to native vehicle autoservice so BYDMate can control comfort
hardware (HVAC, seat heat/vent, mirror fold, sunroof, windows, locks, lights) without going
through the third-party D+ app. Read channel from Phase 1 stays as-is.

## 2. Why

D+ as control intermediary has four problems:

1. **Outside our trust boundary.** D+ is third-party, can OTA-change command surface anytime.
2. **Latency.** D+ proxy adds ~300-700 ms per command vs direct Binder ~10 ms.
3. **No state echo.** D+ does not surface confirmation of write success.
4. **Coverage gaps.** Some comfort fids have no D+ voice command (mirror fold being our
   recently discovered example — operator has hardware button but no D+ surface).

Native write removes all four.

## 3. Scope

### 3.1 Core scope — BYDMate UI parity (REQUIRED)

The baseline is **everything BYDMate's UI currently exposes**, not what D+ happens to
support. Two surfaces compose the baseline:

- **Automation rules** — `AutomationViewModel.ACTION_COMMANDS` (50 commands shown to
  operators in the rule editor).
- **Smart Home / Alice** — VPS device catalog at `/home/ubuntu/bydmate-alice/server.js`
  (18 device entries, each mapped to a Chinese command string).

Their union (with overlap merged) is the **canonical 55-command parity list** below.
Every entry must work via native `setInt` after Phase 2 ships. No D+ fallback at runtime
once migration is complete (Phase 2e exit criterion).

#### 3.1.1 Parity command table

Column **Src** uses: `A` = Automation, `S` = Smart Home, `A+S` = both.

| # | Command (CN) | Category | Surface | Src |
|---|---|---|---|---|
| 1 | `车窗通风` | Windows | Vent all | A+S |
| 2 | `车窗关闭` | Windows | Close all | A+S |
| 3 | `车窗全开` | Windows | Open all | A+S |
| 4 | `车窗半开` | Windows | All to 50% | A |
| 5 | `前排车窗关闭` | Windows | Close front pair | A+S |
| 6 | `后排车窗关闭` | Windows | Close rear pair | A+S |
| 7 | `前排车窗全开` | Windows | Open front pair | A+S |
| 8 | `后排车窗全开` | Windows | Open rear pair | A+S |
| 9 | `主驾打开100` | Windows | Driver open | A+S |
| 10 | `主驾打开0` | Windows | Driver close | A+S |
| 11 | `副驾打开100` | Windows | Passenger open | A+S |
| 12 | `副驾打开0` | Windows | Passenger close | A+S |
| 13 | `后左打开100` | Windows | Rear-left open | S |
| 14 | `后左打开0` | Windows | Rear-left close | S |
| 15 | `后右打开100` | Windows | Rear-right open | S |
| 16 | `后右打开0` | Windows | Rear-right close | S |
| 17 | `自动空调` | HVAC | Auto AC on | A+S |
| 18 | `打开空调通风` | HVAC | Fan-only (AC off) | A+S |
| 19 | `设置温度18` | HVAC | Set temp 18°C | A |
| 20 | `设置温度20` | HVAC | Set temp 20°C | A |
| 21 | `设置温度22` | HVAC | Set temp 22°C | A |
| 22 | `设置温度25` | HVAC | Set temp 25°C | A |
| 23 | `设置温度{N}` 16-32 | HVAC | Any int 16-32 via Alice range capability | S |
| 24 | `内循环` | HVAC | Recirc internal | A+S |
| 25 | `外循环` | HVAC | Recirc external | A+S |
| 26 | `吹前挡` | HVAC | Windshield defrost on | A+S |
| 27 | `关闭吹前挡` | HVAC | Windshield defrost off | A+S |
| 28 | `主驾座椅加热1档` | Seats | Driver heat L1 | A |
| 29 | `主驾座椅加热2档` | Seats | Driver heat L2 | A |
| 30 | `主驾座椅加热关闭` | Seats | Driver heat off | A |
| 31 | `副驾座椅加热1档` | Seats | Passenger heat L1 | A |
| 32 | `副驾座椅加热2档` | Seats | Passenger heat L2 | A |
| 33 | `副驾座椅加热关闭` | Seats | Passenger heat off | A |
| 34 | `主驾座椅通风1档` | Seats | Driver vent L1 | A |
| 35 | `主驾座椅通风2档` | Seats | Driver vent L2 | A |
| 36 | `主驾座椅通风关闭` | Seats | Driver vent off | A |
| 37 | `副驾座椅通风1档` | Seats | Passenger vent L1 | A |
| 38 | `副驾座椅通风2档` | Seats | Passenger vent L2 | A |
| 39 | `副驾座椅通风关闭` | Seats | Passenger vent off | A |
| 40 | `后视镜加热` | Mirrors | Heat on | A+S |
| 41 | `关闭后视镜加热` | Mirrors | Heat off | A+S |
| 42 | `氛围灯打开` | Lights | Ambient on | A+S |
| 43 | `氛围灯关闭` | Lights | Ambient off | A+S |
| 44 | `打开日行灯` | Lights | DRL on | A |
| 45 | `关闭日行灯` | Lights | DRL off | A |
| 46 | `打开车内灯` | Lights | Interior on | A+S |
| 47 | `关闭车内灯` | Lights | Interior off | A+S |
| 48 | `车门上锁` | Locks | Lock all | A+S |
| 49 | `车门解锁` | Locks | Unlock all | A+S |
| 50 | `天窗打开100` | Sunroof | Open 100% | A+S |
| 51 | `天窗打开50` | Sunroof | Open 50% | A |
| 52 | `天窗打开0` | Sunroof | Close | A+S |
| 53 | `遮阳帘打开` | Sunshade | Open | A+S |
| 54 | `遮阳帘关闭` | Sunshade | Close | A+S |
| 55 | `开后备箱` | Trunk | Open | S |
| 56 | `关后备箱` | Trunk | Close | S |

(56 rows due to set-temp split into 4 presets + range row. Logical commands = 55.)

#### 3.1.2 Surface contract

The native dispatcher exposes the **same Chinese command strings** as input — call sites
in `ActionDispatcher` and `AlicePollingManager` change only the transport (`controlClient`
→ `NativeControlClient`), not the strings or call shape. This keeps the rule editor, rule
DB rows, and VPS protocol all unchanged.

Internally each string maps to one or more `(fid, dev, value)` tuples. Composite
commands (e.g. `车窗全开` = open all four windows) issue multiple `setInt` calls back-to-back.

#### 3.1.3 Sunroof under the hood

S10 showed Leopard 3 sunroof returns enum (1=open/2=closed/5=vent) on read. UI keeps
percent-string commands (`天窗打开100/50/0`); the dispatcher maps percent → enum:

- `100` → write enum value for "fully open"
- `50` → write enum for "intermediate" if supported, else fall back to "fully open"
- `0` → write enum for "closed"

If `50` cannot be expressed via enum, dispatcher logs and surfaces `unsupported_position`;
UI continues to show only the presets it can guarantee. No change to the rule editor
options.

### 3.2 Extensions — candidates beyond parity

These are **NOT in the parity baseline**. None block Phase 2 ship. Each is added only
on explicit operator approval, one by one, after Phase 2b probe matrix confirms its
(fid, dev) tuple exists on Leopard 3:

- **Mirror fold / unfold** — operator confirmed hardware button (S10); no D+ command;
  Phase 2b candidate to probe and surface.
- **Fog lights** — front + rear; operator-described as "удобная функция"; no current UI
  surface; needs probe.
- **Hazard (warning blink)** — operator-described as "удобная функция"; no current UI
  surface; visible-to-others — extra confirm prompt required if added.
- **AC dual mode** — split temperature driver vs passenger; operator-confirmed useful;
  no current UI surface; requires both per-zone temp fids on Leopard 3.
- **Hood (front trunk)** — operator noted in S10 it can also open by percent; no current
  UI surface; needs probe of position fid.
- **AC fan level write** — Automation has `FanLevel` as a trigger (read) but no action
  (write); could be exposed as `设置风量{N}` later.

Adding any of these requires:

1. Successful Phase 2b probe (readable fid + writable fid + value mapping verified).
2. Operator approval message in chat.
3. UI surface decision (add `ACTION_COMMANDS` entry, add Alice device, both, or neither).

### 3.3 Hard out-of-scope (NEVER write)

- **Drive controls** — brake, throttle, gear, steering, EPB, ADAS configs.
- **Firmware** — anything writing to `/system`, BMS, MCU, CAN bus, gateway, OTA package.
- **Safety** — airbag arming, ABS thresholds, seatbelt enforcement, alarm state.
- **Powertrain** — drive mode (Eco/Normal/Sport), energy mode, e-pedal mode. UI surfaces
  these as read-only telemetry only.
- **Service intervals / odometer** — anything that could alter resale records.

Enforced at the **allowlist layer** (see §6). Any write to a non-allowlisted (fid, dev)
pair is rejected before reaching native. Hazard from §3.2, if approved, lands in §3.1 +
allowlist together — not silently.

## 4. Architecture challenge

`transact=6` (setInt) on autoservice fails with `SecurityException` when invoked directly
from ADB WiFi shell (validated S2 attempt, see memory
`reference_autoservice_write_channel.md`). Autoservice checks the calling uid against an
internal allowlist; only first-party system processes pass.

### 4.1 Two paths considered

**Path A — DataStream bridge to shell-uid daemon.**
A long-running native helper started via `app_process --nice-name=...` runs under shell
uid (which IS in autoservice's allow-list). BYDMate connects to its TCP loopback via a
small binary protocol (writeByte opcode + 3× writeInt + readByte status + readInt
payload) and forwards write requests. Helper performs `Binder.transact(6, ...)` directly.

Pros: clean, fast (sub-50 ms round trip), survives BYDMate restart if daemon kept alive.
Cons: daemon needs initial activation via on-device ADB shell every cold boot
(self-respawn possible if seeded in user-script start-up).

**Path B — Each-call ADB-on-device shell pipe.**
BYDMate reuses its existing `AdbOnDeviceClient` (827 LOC already in main, used for read
fallback in older diagnostics) to push a single `service call autoservice 6 ...` command
each time.

Pros: zero daemon, easier first prototype.
Cons: ~300-500 ms per call (slower than D+), still needs initial ADB pairing, and breaks
if user toggles "Wireless ADB" off between sessions.

**Recommendation:** Path A. Path B as fallback only.

### 4.2 Daemon lifecycle (Path A)

- **Cold boot:** BYDMate's `BootReceiver` → `WorkManager` queues a one-shot `DaemonStartWorker`
  that does:
  1. `AdbOnDeviceClient` opens `127.0.0.1:5555`, RSA-pairs (one-time).
  2. Pushes `byd-comfort-daemon` binary to `/data/local/tmp/`.
  3. Runs `setsid app_process /data/local/tmp -Xnoimage-dex2oat /system/bin/--nice-name=byd-comfort-daemon com.bydmate.comfort.Daemon &`.
  4. Daemon listens on `127.0.0.1:<port>`.

- **Hot path:** BYDMate process opens TCP socket to daemon, sends `{opcode, dev, fid, value}`,
  reads `{status, readback}`.

- **Restart:** Daemon survives BYDMate process death but dies on Android reboot. BootReceiver
  re-seeds it. Heartbeat from BYDMate; on failure, re-spawn via ADB.

## 5. Safety / constraints

These are **hard rules**, enforced at allowlist + dispatch layers, not optional config.

1. **Speed gate.** If `Speed > 0` km/h (autoservice fid `-1807745016` dev=1013), block:
   windows down (already exists in current `ActionDispatcher` for D+ commands — preserve).
   Allow: AC, seats, lights (safe at any speed). Mirror fold: block above 5 km/h (folds
   only when parked anyway).

2. **Operator first-write gate.** First time the app writes ANY autoservice fid:
   modal "BYDMate will now control vehicle X. Allow? [Once / Always / Never]". Stored in
   per-fid map. Defaults to **off** until operator approves.

3. **Allowlist.** Hard-coded `WRITE_ALLOWLIST` map in code: `(fid, dev) → (value range,
   category)`. Anything outside the list — including category "drive" or "firmware" —
   is rejected pre-dispatch. New entries require code change + review.

4. **Audit log.** Every write call: timestamp, fid, dev, requested value, readback value,
   status. Stored in Room DB `vehicle_write_log` table. Retained 30 days. Surfaced in
   Settings → "Recent vehicle actions".

5. **Readback verification.** After every write, immediately `transact=5` to read the same
   fid (different dev if needed — see Phase 1 read/write namespace split in memory). If
   readback ≠ expected, mark action FAILED in log. UI shows error.

6. **Undo capability.** Maintain `last_known_state` per fid; "Undo" surfaced for 10 s
   after each comfort action. Tap → re-issues opposite write.

7. **Kill switch.** Settings → "Native vehicle control" toggle. Default ON post-onboarding.
   When OFF, all `setInt` writes return blocked status with reason `disabled_by_user`;
   `ActionDispatcher.dispatchParam` and `AlicePollingManager` surface the block to the
   caller (rule log entry, Alice ACK with error). **No D+ fallback at runtime** — D+ is
   removed from the dependency graph at Phase 2e merge. Kill switch is a hard stop, not
   a transport switch.

8. **No batch writes.** One fid per call. No multi-fid Binder transactions. Limits blast
   radius if a malformed call reaches autoservice.

## 6. Read/write fid namespace split

Phase 1 read fids are in `dev=1000` (HVAC namespace) for seats and AC.
Reference catalog suggests write fids live in `dev=1001` with **fid IDs offset by some
delta** and **values shifted by +1** (see memory `reference_autoservice_write_channel.md`).
Examples to verify on first probe:

| Param | Read (dev=1000) | Write (dev=1001?) | Value mapping |
|---|---|---|---|
| seat_heat_D | fid=702545948 → 0..5 | fid=1125122xxx? | value = read + 1 → 1..6 |
| seat_vent_D | fid=702545944 → 0..5 | fid=1125122xxx? | value = read + 1 |
| ac_temp_main | fid=1077936168 → °C | dev=1001 same fid? | direct °C |

First task in Phase 2b prototype: **write probe matrix** — try `setInt` against candidate
(fid, dev) pairs with a known harmless value (e.g. seat heat current level — write same
value as read), check readback, confirm "shift +1" hypothesis on a few entries before
generalizing.

## 7. Smart Home / Alice integration

Current path: Yandex Alice → VPS Express → BYDMate polling (2.5 s) → `DiParsControlClient.sendCommand` (HTTP localhost:8988 → D+).

Phase 2 changes the **final hop only**: VPS still sends the same Chinese command string;
`AlicePollingManager` calls `NativeControlClient.sendCommand(cmd)` instead of
`DiParsControlClient.sendCommand(cmd)`. The mapping from Chinese string → `(fid, dev,
value)` tuples lives in `NativeControlClient` (one place to maintain).

VPS API unchanged. Alice device catalog (`server.js`) unchanged. All 18 devices keep
their existing commands per the parity list in §3.1.1.

Latency win for Alice users: ~500 ms saved per command (HTTP-loopback proxy removed,
single Binder hop instead).

No D+ fallback at runtime. If the native channel reports a transient failure (daemon
down, readback mismatch), the command is acked to VPS as failed; operator sees standard
Alice error in the app. Recovery: daemon respawn on heartbeat failure (§4.2).

## 8. Phases

| Sub-phase | Description | Files touched | Exit criteria |
|---|---|---|---|
| 2a | This spec + brainstorm | docs only | operator approval |
| 2b | Daemon prototype (Path A): write probe matrix, value-shift validation | new daemon source + Mac harness; no app changes | Read-back matches write for ≥3 fids of each category |
| 2c | BYDMate IPC to daemon: TCP client, allowlist enforcement, audit log, kill switch | `app/src/main/kotlin/com/bydmate/app/data/autoservice/write/*.kt` (new package) + DB migration for `vehicle_write_log` | dev-only debug UI can toggle each comfort param; readback green; speed gate enforced |
| 2d | Migration: `ActionDispatcher` swaps `DiParsControlClient` → `NativeControlClient`; full 55-command parity table (§3.1.1) covered; rule editor unchanged | ActionDispatcher, NativeControlClient (new) | All 50 Automation commands work via native; readback green; latency <100 ms |
| 2e | Alice migration: `AlicePollingManager` swaps client to native; D+ removed from dependency graph | AlicePollingManager, Hilt module | All 18 Alice devices work via native; D+ uninstall does not break BYDMate; `rg "DiParsControlClient"` returns 0 hits app-side |

Each sub-phase merges to `feature/native-data-stack` separately; production release only
after 2c completes with green readback over 100 ops with no operator panic.

## 9. Open questions (for brainstorming)

1. **Daemon binary distribution.** Bake `byd-comfort-daemon` into APK and push from app
   to `/data/local/tmp/` on first run? Or ship as separate split-APK? Single-APK keeps
   install simple; split-APK keeps daemon updatable independently. Lean: single-APK first.

2. **Daemon survival across DiLink ADB toggle.** If operator disables Wireless ADB on the
   head unit, daemon dies and can't be respawned. Per [[feedback-adb-always-enabled]]
   ADB stays on for our user base, but if it ever goes off: prominent banner "Native
   control disabled — Wireless ADB is off" + kill-switch behavior (commands rejected,
   not silently fallback). No D+ fallback because we are removing D+. OK with this?

3. **Permission UI.** First-time write modal — show once per category (AC / seat /
   mirror / window / lock / light / sunroof / trunk) or once per fid? Recommendation:
   per category (8 prompts max, not 55).

5. **Trunk control.** Currently used via Alice (`开后备箱` / `关后备箱`). Native
   implementation can be one-shot enum write, but the hardware actually moves slowly —
   should the readback wait until the trunk physically stops (~5 s)? Or return success
   on Binder ack and let UI poll state separately? Recommendation: ack on Binder,
   non-blocking, UI shows transient "opening…" state.

6. **Lock/unlock from app.** Operator-sensitive — accidentally locking out from outside
   is plausible. Should writes to locks require **biometric reauth** even after first-write
   approval? Recommendation: yes for `车门上锁` (locking), no for `车门解锁` (unlocking is
   safe — worst case is unintended unlock when phone in pocket, mitigated by app
   foreground requirement).

7. **Window position granularity.** Current command surface is enum-by-percent
   (`主驾打开100` / `主驾打开0`); no intermediate value in either Automation or Alice.
   Keep enum-only on native side (mirror current UX) or add free-percent write
   (`主驾打开{N}`) as a 2c bonus? Recommendation: enum-only for parity, free-percent
   parked as §3.2 extension if requested.

8. **Audit log retention.** 30 days enough or do we want longer? Storage minimal but
   privacy consideration if vehicle changes owner.

9. **Extension prioritization.** §3.2 lists 6 candidates (mirror fold, fog, hazard, AC
   dual, hood, AC fan write). After 2b probe matrix, which order to ship and via which
   surface (Automation rule editor, Alice device, or both)? Decide at 2b → 2c gate.

## 10. Risks

| Risk | Severity | Mitigation |
|---|---|---|
| Daemon disabled by autoservice update (OTA) | HIGH | Detect early via readback failure; ship in-app banner explaining the break; operator can hold off on OTA until update available |
| Wrong (fid, dev, value) triggers undefined hardware state | CRITICAL | Allowlist + readback + manual probe matrix in 2b before generalizing |
| Privacy: audit log of operator actions leaks | LOW | Stored locally only; never sent to VPS or telemetry |
| First-write modal user fatigue | MEDIUM | Per-category not per-fid; default "Always" option |
| Daemon respawn loop on broken ADB pairing | LOW | Exponential backoff; max 3 retries before going dark with notification |
| Regression on parity command — one of 55 stops working post-migration | HIGH | 2d/2e exit checklist runs each of the 55 commands once on real Leopard 3 before merge; missing readback = block merge |

---

## Appendix A — Reference points

- Memory: [[reference_autoservice_write_channel]] — namespace split, value shift, DataStream pattern
- Memory: [[reference_autoservice_binder]] — Binder interface token, transact codes
- Memory: [[reference_seat_heat_vent_fids_leopard3]] — validated dev=1000 read fids
- Memory: [[reference_adb_on_device_pattern]] — embedded ADB client in BYDMate
- Code: `app/src/main/kotlin/com/bydmate/app/data/autoservice/AdbProtocolClient.kt` (existing)
- Code: `app/src/main/kotlin/com/bydmate/app/data/autoservice/AutoserviceClient.kt` (existing read path; needs write companion)
- Doc: `docs/native-data-stack/fid-map.md` — full S2-S10 validation table

## Appendix B — Decisions deferred to brainstorming

The 8 open questions in §9 + the 6 risk mitigations need explicit operator answers before
2b prototype. Brainstorming should produce one-line resolution per question.
